model_list:
  - model_name: "claude-3-opus-local"   # 对外暴露的模型名（给 Claude Code 看的）
    litellm_params:
      model: "ollama_chat/gemma4:e4b"       # 实际调用的本地模型
      api_base: "http://localhost:11434"
      api_key: "none"
    model_info:
      supports_function_calling: true

litellm_settings:
  drop_params: true