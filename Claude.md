# 全局 Claude 配置

## 行为准则
- 修改代码前先理解整体架构
- 不要在没有确认的情况下进行大规模重构
- 每次修改后暂时不需要运行相关测试

## 代码风格
- 优先使用函数式编程
- 变量命名使用 camelCase（JS/TS），snake_case（Python）
- 注释使用英文，代码使用英文

## 多项目工作规范
- 工具调用时需要考虑当前系统是windows
- 在开始任何任务前，先读取项目根目录的 CLAUDE.md 
- 读取 .claude/context/ 下的上下文文件以了解项目历史
- 更新 ROADMAP.md 的任务状态（[ ] → [-] → [x]）
- 当我进入到某一个子项目的文件夹中时，需要再检查一下子文件夹中的CLAUDE.md，它是对当前子文件夹中的子项目的描述